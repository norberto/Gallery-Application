<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:b="http://www.springframework.org/schema/beans"
         xsi:schemaLocation="http://www.springframework.org/schema/beans
                             http://www.springframework.org/schema/beans/spring-beans.xsd
                             http://www.springframework.org/schema/security
                             http://www.springframework.org/schema/security/spring-security.xsd">

    <b:bean id="authProvider"
          class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
        <b:property name="userDetailsService" ref="customUserDetailsService" />
        <b:property name="passwordEncoder" ref="encoder" />
    </b:bean>

    <b:bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <http auto-config="true" use-expressions="true">
        <form-login login-page="/login.zul"
                    always-use-default-target="true"
                    default-target-url="/view.zul"
                    authentication-failure-url="/login.zul?error"
        />
        <logout     logout-url="/logout"
                    logout-success-url="/login.zul?logout" />


        <intercept-url pattern="/index.zul*" access="permitAll"/>
        <intercept-url pattern="/register.zul*" access="permitAll"/>
        <intercept-url pattern="/view.zul" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/upload.zul" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/browse.zul" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/zkau/**" access="permitAll"/>
        <intercept-url pattern="/zkau**" access="permitAll"/>
        <intercept-url pattern="/zkau/upload**" access="permitAll"/>



        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="encoder" />
        </authentication-provider>
    </authentication-manager>



</b:beans>